angular.module('PCMS').controller('CommonSelfBuildController',
		[ '$scope', '$http', function($scope, $http) {
			$scope.title = '3345678';
			
			$scope.init = function() {
			}
			
			$scope.init();
		} ]);